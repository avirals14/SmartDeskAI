# Used by docker-compose.yml for the backend and worker containers.
# Copy this file to .env (same folder as docker-compose.yml) before running
# `docker compose up`.

JWT_SECRET=change-this-to-a-long-random-string
GROQ_API_KEY=
